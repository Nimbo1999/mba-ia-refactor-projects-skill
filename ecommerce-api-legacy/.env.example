PORT=3000
NODE_ENV=development

# Database (SQLite in-memory for this project, kept for parity with a real DB setup)
DB_USER=changeme
DB_PASSWORD=changeme

# Payment gateway
PAYMENT_GATEWAY_KEY=changeme

# SMTP
SMTP_USER=no-reply@example.com

# Token required to access protected admin endpoints
ADMIN_TOKEN=changeme
